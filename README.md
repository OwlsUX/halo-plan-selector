
  # Drew's draft

  This is a code bundle for Drew's draft. The original project is available at https://www.figma.com/design/CTqVecLlmLHbkvNWhPh7e7/Drew-s-draft.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  